rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lotteryResults/{document=**} {
      // Data is public and writes are open for this example.
      // In a production app, you would restrict writes.
      allow read, write: if true;
    }
  }
}
